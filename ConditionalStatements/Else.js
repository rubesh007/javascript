
/* CONDITIONAL STATEMENTS */
// ELSE Condition
// if the ( IF ) condition fails it will run the else condition
let isMajor;
let age = 10;
if(age >18){ // if this condition fails it will automatically run the block of ELSE condition
    console.log(isMajor = true) // here the condition fails so, this block wont run 
}
else{
    console.log(isMajor = false) // => this condition will execute dircetly if the ( IF ) condition fails
}


